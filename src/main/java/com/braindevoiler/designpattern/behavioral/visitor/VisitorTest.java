package com.braindevoiler.designpattern.behavioral.visitor;

public class VisitorTest {
    /**
     * Use the Visitor Pattern when you want to add capabilities to a composite of objects(Composition pattern) and
     * encapsulation is not important.
     * <p>
     * It allows you to add operations to a Composite structure without changing the structure itself.
     * <p>
     * Adding new operations is relatively easy.
     * <p>
     * The code for operations performed by the Visitor is centralized.
     * <p>
     * The Composite classes' encapsulation is broken when the Visitor is used.
     */
    public static void main(String[] args) {

    }
}
